<h1>Create a Creature</h1>

<%= form_for :creature,  url: {action: "create"} do |f| %>

    <%= f.text_field :name %>
    <%= f.text_area :description %>
    <%= f.submit "save creature" %>

<% end %>